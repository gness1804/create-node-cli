{
  "name": "{{name}}",
  "description": "{{description}}",
  "version": "{{version}}",
  "author": "{{authorName}}",
  "type": "module",
  "bin": {
    "{{name}}": "index.js",
    "{{command}}": "index.js"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "fix": "eslint --fix . && prettier --write .",
    "start": "node index.js"
  }
}