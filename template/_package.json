{
  "name": "{{name}}",
  "description": "{{description}}",
  "version": "{{version}}",
  "author": "{{authorName}}",
  "bin": {
    "{{name}}": "index.js"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "fix": "eslint --fix . && prettier --write ."
  },
  "pre-commit": [
    "lint"
  ]
}