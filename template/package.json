{
  "name": "{{name}}",
  "description": "{{description}}",
  "version": "{{version}}",
  "bin": {
    "{{name}}": "index.js"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "fix": "eslint --fix . && prettier --write ."
  },
  "pre-commit": [
    "lint"
  ]
}